import styled from "@emotion/styled";
import { Button, IconButton, Box } from "@mui/material";

export const StyledButton=styled(Button)` border-radius: 1rem;
margin-bottom: 2rem;
text-transform: none;
font-weight: 800;
`;

export const StyledIconButton=styled(IconButton)` color: var(--main-color);
`;

export const StyledCardHeader=styled(Box)` height: 1rem;
background-color: var(--main-color);
`;